{
  "name": "client",
  "homepage": "https://ilyachernuha.github.io/hyper-store/",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist -f && git commit && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "lazysizes": "^5.3.2",
    "swiper": "^10.0.2"
  }
}
